apply plugin: 'java'

dependencies {
    compile fileTree(dir: '../../../../lib', include: 'dexguard-api.jar')
}

sourceSets {
    main {
        java.srcDir 'src'
    }
}

jar {
    destinationDir = projectDir
    archiveName    = 'lib/sample-plugins.jar'
}
